<% include header %>

<app layout="row">
	Loading...
</app>

<script type="text/javascript">
	var appConfig = {
		slug: '<%= locals.slug %>',
		clientBasePath: '<%= locals.clientBasePath %>',
		assistUrl: '<%= locals.assistUrl %>',
		assistCredentials: '<%= locals.assistCredentials %>',
		apiUrl: '<%= locals.apiUrl %>',
		apiToken: '<%= locals.apiToken %>',
	};
</script>

<% if (locals.environment !== 'production') { %>

	<script type="text/javascript" src="/dev/js/bundle.js?v=<%= locals.version %>"></script>

<% } else { %>

	<script type="text/javascript" src="<%= locals.clientBasePath + slug %>/build/js/bundle.js?v=<%= locals.version %>"></script>

<% } %>

<% include footer %>
